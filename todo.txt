Implement the feature on mails in which when we click on any mail
it will show the sender, subject and body of the mail.

The above feature is Implemented but it's showing the mail in the console
So I need to modify this feature to display the mail on the display.

    Work is in progress.
        Right now I have Implemented to display the json object on display
        which I need to modify to look better
        Right Now The information is now visible on the display but didn't done
        much styling and also needs a button on that page for reply